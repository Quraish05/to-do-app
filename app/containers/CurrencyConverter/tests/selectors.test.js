// import { selectCurrencyConverterDomain } from '../selectors';

describe('selectCurrencyConverterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
